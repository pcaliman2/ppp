import 'package:flutter/material.dart';

class OWATherapiesPage extends StatelessWidget {
  const OWATherapiesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Therapies')),
      body: const Center(child: Text('Therapies Page')),
    );
  }
}
