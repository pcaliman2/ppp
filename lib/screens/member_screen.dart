import 'package:flutter/material.dart';

class OWAMemberPage extends StatelessWidget {
  const OWAMemberPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Become a Member')),
      body: const Center(child: Text('Member Page')),
    );
  }
}
