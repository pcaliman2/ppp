import 'package:flutter/material.dart';

class OWASciencePage extends StatelessWidget {
  const OWASciencePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Science')),
      body: const Center(child: Text('Science Page')),
    );
  }
}
