import 'package:flutter/material.dart';

class OWABookSessionPage extends StatelessWidget {
  const OWABookSessionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Book a Session')),
      body: const Center(child: Text('Book Session Page')),
    );
  }
}
